package monsters;

import weapons.Weapon;

public class Entity {
    int ac;
    int speed;
    int hp;
    int maxHP;
    Weapon[] weapons;
    int pos;
    public String Attack(Entity target, int roomRadius) {
        if (maxHP/hp > 2) {
        }
        for (Weapon i : this.weapons) {}

        return "";
    }
}
